# AGENTS.md

The instructions for this repository live in **[CLAUDE.md](CLAUDE.md)**. Read that file before making changes.

It is kept as the single source of truth so that Claude Code and Codex share one set of instructions instead of two copies that drift apart. This file is only a pointer: do not add guidance here, add it to `CLAUDE.md`.
